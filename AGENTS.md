## Encoding Rules

- 所有文件读写一律使用 UTF-8（无 BOM）。
- 禁止使用默认编码、GBK、ANSI。
- 执行任何写文件命令前，必须先检查并确认控制台编码为 UTF-8（`chcp 65001`）。
- 读写文件时必须显式指定 UTF-8 编码。
- 如发现中文乱码（例如“绔炶禌 Service 涓氬姟灞傚鐞?”），不得直接修乱码文本，必须先确定原文（从正确源文件、历史版本或上下文）再替换。
- 若无法确认原文，必须先询问再修改。
- 违反以上规则会再次破坏编码，必须严格遵守。